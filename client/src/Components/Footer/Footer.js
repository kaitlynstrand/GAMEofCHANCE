import React from "react";
import "./Footer.css"

const Footer = props =>
<section className="hero" id="footer">
  <div className="hero-body">
      	    {props.children}
  </div>
</section>

export default Footer